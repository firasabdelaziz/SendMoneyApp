{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "typeRoots": [
      "./node_modules/@types",
      "./src/types/",  // or "./types" or "./@types" depending on your folder structure
    ],
    "strict": true
  }
}
